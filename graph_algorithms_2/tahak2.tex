\documentclass[8pt]{extarticle}

\usepackage[utf8]{inputenc}
\usepackage[czech]{babel} % Správné české dělení slov a znaky
\usepackage[T1]{fontenc}

% --- GEOMETRIE: A4 papír s minimálními okraji ---
% Okraje nastaveny na 5mm, což je hranice, kterou zvládne 99 % běžných tiskáren bez oříznutí textu.
% \usepackage[a4paper, margin=10mm, noheadfoot, landscape]{geometry} 
\usepackage[a4paper, left=43.7mm, right=43.7mm, top=31.2mm, bottom=31.2mm, noheadfoot, landscape]{geometry}
% Vynulování skrytých systémových mezer hlavičky, patičky a horního okraje
\setlength{\headheight}{0pt}
\setlength{\headsep}{0pt}
\setlength{\topskip}{0pt}
\setlength{\footskip}{0pt}

\usepackage{multicol} 
\usepackage{amsmath, amssymb, amsthm}
\usepackage{mathptmx} % Písmo Times - velmi kompaktní, klasické novinové sázející natěsno
\usepackage[protrusion=true,expansion=true,tracking=true]{microtype}

% Extrémní smrsknutí mezer uvnitř matematických vzorců
\thickmuskip=1mu \medmuskip=1mu \thinmuskip=1mu 

% ZÁBRANA ZALOMENÍ VZORCŮ (Matematické věci zůstanou striktně na jednom řádku)
\binoppenalty=10000
\relpenalty=10000

% Vynulování vnitřních mezer mezi odstavci a řádky
\pagestyle{empty} 
\setlength{\parindent}{0pt} 
\setlength{\parskip}{0pt}   
\setlength{\parsep}{0pt}

% Drastické zúžení řádkování a mezer mezi sloupci
\renewcommand{\baselinestretch}{0.72} 
\setlength{\columnsep}{1mm} 

\begin{document}
% Extrémně malé písmo (4.8pt) s adekvátním řádkováním (5.2pt) pro maximální hustotu
\fontsize{4.1pt}{4.8pt}\selectfont 

% Využíváme celou plochu A4 na šířku, takže 5 nebo i 6 sloupců je teď ideální
% \begin{multicols*}{5} 
\begin{multicols*}{4} 


% =======================================================================
ALG BOYERA-MYRVOLD: KRESLENI G DO ROVINY: Vstup: neorient G, Vystup: "je rovinny" + vyda nakresleni nebo "neni rovinny" a da dk kuratowskeho podgrafu. Cas $O(n)$. Z dfs - pouze stromove a zpetne hrany; dopredne a pricne nejsou. dfs strom je kostra G. Relace "hrany $e$ a $f$ lezi na spolecne kruznici" ($e\sim f$) je ekviv. Tridy jsou max 2-souvis podgrafy (bloky), ekv trid s jednou hranou (most) = trivialni blok, vrch $v$ je artikulace $\equiv$ sousedi hran z jinych bloku. Sitauce v dfs $u\rightarrowtail v\rightarrowtail w_1\_w_k$,$\odot$ zpetne $\overleftarrow{xv}$ vzdy $\overrightarrow{vx}\sim \overrightarrow{uv}$. $\odot$ if $\overrightarrow{uv} \sim \overrightarrow{vw_1}$, musi $\exists$ zpetna hrn $f$ z $T(w_1)$ nad $v$. $\odot$ if $vw_i\sim vw_j$, musi byt $\sim$ pres $uv$, lebo $\not\exists$ pricne hrn, if $\not\exists$ ani $uv$, tak nejsou $\sim$, napr $v$ je koren. 
Pri dfs testuji $\exists$ zpet hrn: $Enter(v)$, $Ancestor(v)=$ min z $Enter(u)$, kdy $vu$ je zpet hrn, $LowPoint(v)=$ min z $Anc(x)$: $x\in T(v)$. $\odot$ lze spocitat v $O(n)$ pri dfs. Lemma: $u\rightarrowtail v\rightarrowtail w_1\_w_k$ v dfs strmu, pak strm hrany $uv\sim vw_i \equiv LowP(w_i)<Entr(v)$ a $v$ je artikulace $\equiv$ $\exists i: LowP(w_i)\geq Entr(v)$ a koren dfs je artikulace $\equiv$ \# jeho synu $> 1$. Kresleni: vrcholy od nejvyssiho $Entr$, udrzujeme blokovou $B_1, B_2..$ usporadanou strukturu jiz nakreslene casti, blok s vyssimi $Entrs$ driv, $\forall B_i$ ma svuj koren (minimalni $Entr$ v $B_i$), ten je artikulaci v nadrazenem $B_i+1$ (krome nejvyssiho blku); $\forall$ koreny, ktere jsou artikulace cp $\rightarrow$ lepsi repr v pameti a preklapeni v $O(1)$. $\forall B_i$ jdou nakreslit ohranicene kruznici, krom mostu ($B$ je jen hrana) $\rightarrow$ zdvojime hranu na 2-cyklus. $\odot$ $B_i$ lze preklopit dle koren artikulace (i se vsemi pod nim), neporusi rovinnost. Invariant: $\forall$ nenakreslene hrany vedou do vrcholu s mensim $Entr$ (lebo postupujeme nejvys $Entr$). Dusledek $\forall$ externi vrcholy (ty ke kterym jeste neco budem pripojovat), musi lezet ve stejne stene dosud nakresleneho podgrafu, kdyby vrchol dostal do jine uzavrene steny, uz se k nemu z vnejsku nedostanem. Buno tato stena je ta vnejsi; Zpracovani $v$: potreba pridat $\forall$ hrany do nasledniku (dfs), tedy vrcholy co uz nakreslene: 1. strm hrny - jdou vzdy pridat jako trivi bloky (2-cykly) 2.zpet hrny - tyto vrchy byly exter, nakreslenim hrny nahradime cestu po okraji a muze i sloucit bloky do jednoho, $\odot \forall$ hrna muze zmizet z vnejsi steny max 1x $\rightarrowtail$ amort. $O(1)$, if $\exists$ exter vrchol na odriznute ceste, musime preklopit dotcene blky. Vrchol $w$ je exter pri zpracovani $v$ $\equiv$ $Ancstr(w) < Entr(v)$ or if $\exists$ blok s aspon jednim vrcholem, tedy:$\exists x: LowP(x) < Entr(v)$, x je syn $w$ \& $x$ v jinem bloku, nez $w$ (2. plati, protoze koren bloku (tady $w$) ma max jednoho dfs syna, jinak by $\exists$ pricna hrna, proto na check blokuu, kterych je $w$ koren se staci podivat na prvniho syna v tomto bloku). Test if $v$ je exter v $O(1)$: Df: $BlockList(w)=$ seznam bloku pripojenych v danem okamziku pod $w$, repr koreny blku (klony $w$) a vzdy jedinym synem korene, vzestupne setrideny dle $LowP$ toho syna. Lemma: Vrch $w$ je exter, if $Ancstr(w) < Enter(v)$ or prvni prvek $BlockList(w)$ ma $LowP < Enter(v)$, a $BlockList$ lze udrzovat amort $O(1)$. Dk: 1. z def a 2. na zacatku pro $\forall$ trivialni bloky vytvorime $BlockListy$ pomoci bucketsort v $O(n)$, kdykoliv dojde ke slouceni blokuu, musime vymazat podrazeny slouceny blok z $BlockL$ prislusne artikulace ($v$ je artikulace $B_1$ a jeho klon $v'$ koren $B_2$, $BlockL$ je ulozen ve $v$) odstranime v $O(1)$ pomoci ptru, ktery ukazuje do $BlockL$, kazdy blok odstranen za beh alg max 1x a bloku je $O(n)$$\square$. Obchazeni hranice stejnym smerem $\rightarrow$ $\forall$ vrcholy na hranici, dva ukazatele, na sousedy, prijdu-li jednim, jdu druhym. $\forall$ $B_i$ si pamatuje orientaci (preklapeci bit ulozen v koreni) a zajima nas uz jen vnejsi hranice. Preklopeni $B_i$ a $\forall$ pod nim, pouze negace bitu. Preklopeni pouze $B_i \rightarrow$ negace bitu a projiti hranice a negace bitu v korenech podrazenych bloku.|Repr v pameti: pro $\forall v$ mame obousmerny spojak sousedu odpovidajici poradi, kdyz jdeme po smeru hodinovych rucicek. Zapojeni nove hrany za behu $\rightarrow$ musime znat abs orientaci tohoto seznamu, pri fazi WalkDown si udrzujeme abs orientaci.|$WalkUp$ (znaceni ziveho G):  zpracovavam $v$, chci nakreslit zpetne hrany z potomku $v$ do $v$, naivne pri prohledani podstromu az $O(n) \rightarrow$ $WUp$ pro $\forall$ vrcholy $\rightarrow$ $O(n^2)$ :(. Omezime na zivy podG, 3 typy $v$: Zivy= $\exists$ zpet hrna do aktual zpraco $v$ or je pod nim pripojen zivy blok, Externi=.., Pasivni= neni zivy ani exter. (vrchol muze byt ziv i exter, pokud $\exists$ zpetna hrna do i nad $v$). Oznaceni: 1. $\forall$ zpet hrny $xv$ do $v$ oznacime $x$ zive (sousedi v puvodnim grafu, kteri maji $Entr(x)>Entr(v)$),2. need ozivit artikulaci nad blokem prave oziv vrchu (a opakovat az k $v$),3.Paralelni pruchod: jdeme po hranici obema smery, oznacujeme visited a az najdeme koren, ulozime ptr do visited na koren. Dk, ze slozitost $WUp$ pres $\forall$ vrcholy $\in O(n)$: paralel hledani stoji $2k$, $k=$ delka kratsi cesty ke koreni, nakreslenim hrany spadne tato cesta do vnitrni steny, cas hledani uctujeme hrnam, kazda hrna se do vnitrku dostane a taky je nakreslena max 1x, $m\leq 3n-6 \rightarrow O(n)$.| $WalkDown$ (kresleni hran): po oznaceni ziveho podG pro vrchol $v$ muzeme kreslit: $\odot$ nakreslenim hrany zpetne hrny do $v$ nemuzeme nikdy propojit podstromy synu $c_1\_c_k$ pod $v$ $\rightarrow$ zpracovavame podstromy synu zvlast. Pro syna $c_i$: 1. nakreslime stromovou hranu jako triv blok (2-cykls), k tomuto bloku vznikne kopie $v$, tedy $v^{c_i}$, 2. z $v^{c_i}$ obchazime hranici obema smery po dosud nakreslene casti, 3. pasiv vrcholy preskakujeme, 4. u externiho zastavime (nejdrive zpracujem, pokud byl i zivy),5. na zive aplikujeme dve pravidla: $P_1$ - kdyz jsme v zivem vrcholu,1. nakreslime zpetne hrany z neho do $v$,2. zanorime se do zivych internich bloku, 3. nakonec 
se zanorime do podrizenych zivych externich bloku. (Algoritmicky to uděláme tak, že máme seznam podřízených bloků setříděné tak, aby externí živé bloky byly až na konci) %TODO
$P_2$ - zanorili jsme se do bloku a resime, zda jit doleva nebo doprava po hranici, volime smer k blizsimu zivemu internimu vrcholu, jinak k zivemu externimu (pomoci look-ahead, jdeme obema smery zaroven), zkontrolujeme, zda ten smer, kterym chceme jit se lisi od smeru, kterym jsme do ted prochazeli graf, pokud ano blok preklopime v $O(1)$.| U $WDown$ se muze stat, ze jdem po dlouhych usecich pasivnich vrcholu, proto jakmile projdeme, udelame zkratkovou hranu, ty na konci algo odstranime.|
Cely Algo: vstup neorient $G$, 1. if $m>3n-6$ $\rightarrow$ Nerovinny! 2. dfs spocitame pro $\forall v:$ $Entr$, $Ancstr$, $LowP$, 3. sestrojime $BlockL$ pro $\forall v$ prihradkove, 4. hlavni cyklus (prochazime vrcholy v poradi klesajicih $Entr$): A: strom hrany- nakreslime z $v$ dolu k jeho synum jako 2-cykly, B: $WalkUp$: oznacime zivy podgraf paralel obchazenim, C: $WalkDown$ pro $\forall$ syna vrcholu $v$ obchazime v obou smerech zivy podgraf a kreslime zpetne hrany do $v$ a striktne se ridime p4avidly $P_1$ a $P_2$ a zastavujeme se o exter vrcholy, D: test neuspechu: zkontrolujeme zda $\forall$ zpetne hrany byly v tomto kroku nakresleny, if not ohlasime Nervoinny! 5. final pruchod: if cyklus uspesne skonci, projdeme hotove nakresleni jednim dfs od absolut korene a zorientujeme seznamy sousedu a hranic. Vydame rovinne nakresleni.| Thm: Algo bezi v $O(n)$. If $G$ rovinny, vyda jeho korektni nakresleni, jinak spravne ohlasi nerovinnost. Dk (casu): 1krok korektni a dale muzeme predopkladat $m=O(n)$, 2krok jiz diskutovali a vytvoreni $BlockL$ je v $O(m)$, 4A: $O(1)$, 4B: paralelni obchazeni z obou smeru smerem ke koreni trva umerne dlouho tomu kolik hran po pridani hrany z vnejsi steny zmizi, protoze kazda hrana muze z vnejsi steny (stat se vnitrni) zmizet pouze 1x za cely beh algo je $O(n)$ celkova slozit vsech $WalkUp$, 4C: analogicky dity shortcut hranam pro preskakovani pasivnich useku, 5: jeden pruchod dfs $\Rightarrow O(n)$, Dk(korektnosti): staci dokazat lemma, protoze stromove dfs hrany sjou vzdy nakreslitelne: lemma - if $\exists$ zpetna hrana, kterou algo nenakreslil, graf na vstupu neni rovinny. $\square$ 
VERIFIKACE KOSTRY: da se efektivneji nez najit min kostru. Staci nam, aby hrany sly porovnavat. $T[u,v]=$ cesta v kostre $T$ z $u$ do $v$. Df: Hrana $uv \not\in T$ je $T$-lehka: $\exists f \in T[e]: w(f) > w(e)$. Thm: $T$ neni minimalni $\equiv$ $\exists e$ ktera je $T$-lehka. Staci porovnat $e$ s nejtezsi hranou na $T[e]$. Potrebujeme algo pro Cestova Maxima: Vstup = strom $T$, vahy $C$ a mnozina dotazu $Q$ (to budou cesty v $T$ urcene koncovymi vrcholy) ($q = |Q|$), Vystup = pro $\forall P \in Q$ netezsi hrana na $P$, chceme algo s $O(n+q)$ porovnani vah. Pokud ho budeme mit, staci za $Q$ dosadit vsechny koncove vrcholy vsech hran mimo $T$ a mame algo verifikaci mst v $O(n+m)$.| Udelame Boruvkuv Strom: spustime Boruvku na $T$ ($=B(T)$)! V kazde fazi se vrcholy slucuji do komponent, tento proces zaznamename do $B(T)$, listy $B(T)$ jsou puvodni vrcholy $T$, vnitrni uzly reprezentuji komponenty, pokud se spoji dve komponenty/vrcholy v $T$, vytvorime jim spolecneho rodice v $B(T)$ (pokud jiz ma jeden z prvku rodice, pripojime druhy prvek k rodici), vahu hrane v $B(T)$ odpovida vaze hrany, se kterou byly komponenty spojeny. Hloubka $\leq Ln$, na hladine $i$ je $\leq n/2^i$ vrcholu. Boruvka na stromech v $O(n)$. Thm $\forall x,y \in V(T): w(\text{max}T[x,y]) = w(\text{max}(B(T)[x,y]))$. Diky tomu muzeme dotazovat $B(T) \rightarrow$ dotaz v $O(2Ln) \Rightarrow \forall$ dotazy $\in O(n)$. Dk: 1.$x,y \in V(T)$, $P=T[x,y]$, $h = (a,b) =$ nejtezsi hrana $\in P$, $P' = B(T)[x,y]$, $h'=$ nejtezsi $\in P'$, $h$ ma svuj protejsek na $P'$, tedy na $P'$ je hrana, ktera odpovida $h$ $\rightarrow$ $w(h) \leq w(h')$. Najdeme vrchol $u$ nejnizsi v $B(T)$, ktery $\{a,b\} \subset C(u)$ ("kdy poprve se $a$, $b$ dostaly do stejne komponenty) $\rightarrow$ urcite $\exists v_a, v_b$ ruzne deti $u$ t.z. $a\in C(v_a)$ a $b \in C(v_b)$, aspon jedna z hran $uv_a$ nebo $uv_b$ v $B(T)$ je $(a,b)$, protoze propojuje komponenty $v_a$ a $v_b$, kdybychom je nepropojily pre $(a,b)$ tak $T$ neni strom. Buno $(v_a, u)$ je kopii $(a,b)=h$. Chceme ukazat, ze pres $(v_a,u)$ vede $P'$: kde lezi $x,y$? Buno $x,y$ jsou pod $v_a$, vime ze $P$ je jedina, komponenta $C(v_a)$ je souvisla $\Rightarrow P \subset C(v_a)$, spor s def $u$. 
Ani $x$ ani $y$ ne pod $v_a$: $P$ pouziva $(a,b)$, ale $x,y$ mimo, tedy $P$ musi nekde "vlezt" a "vylezt", $\exists$ 2 hrany cesty $P$, ktere vedou mezi $C(v_a)$ a $\overline{C(v_a)}$, alespon jedna neni $ab$, $ab$ je nejtezsi a tedy druha lehci, proto by algoritmus vybral druhou misto $ab$ a spojil $v_b$ a $v_a$, spor. Proto pod $v_a$ lezi prave jeden z $x,y$, a ten druhy pod $v_b$ $\Rightarrow$ $P'$ vede pres $v_au$. 2. $\forall u,v \in P'$ (kde $u$ otec $v$): $C(v)$ je incidentni s aspon 1 hranou $f \in P$, pak nastanou dva priklady: A- algo $B$ si vybral $uv$ misto $f \Rightarrow w(uv) \leq w(f) \leq w(h)$ (kazda hrana na $P'$ je lehci nebo stejne tezka jako na $P$), B- nebo $uv=f \Rightarrow w(h') \leq w(h)$ (Proto i nejtezsi hrana na $P'$ je nejvic tak tezka jako nejtezsi na $P$). $\square$
Redukce dotazu: dva typy cest $pricne$ a $svisle$ $\rightarrow$ zbavime se pricnych rozstrizenim na 2 svisle $\rightarrow LCA(x,y)$ a tam rozdelime cesty.
KOMLOSUV ALGO: $Q_e=\{Q_i dotaz | e\in Q_i\}$ (dostaneme zadarmo, resime slozitost v \# porovnani), $top(Q_i)=$ vrsek dotazu, $bottom(Q_i)$, $T_e=$ vrsky vsech cest z $Q_e$, kazdy max 1x a setridene shora dolu; $P_e=$ castecna maxima cest, $P_e[i]=$ max $T[T_e[i],v]$. $\odot w(P_e[i-1]) \geq w(P_e[i])$. $FindPeaks(u, p, T_p, P_p)$: ($u$ je aktual vrchol, $p$ hrana z rodice po ktere jsme do $u$ prisli) 1. Uzavre dotazy se spodkem $u$ $\rightarrow$ odpovime primo dle $T_p$, $P_p$. 2. $\forall$ deti $v$ vrcholu $u$ (pres $e$): - sestrojime $T_e$ z $T_p$ (cast smazeme pro cesty, ktere nejdou pres $p$ a $e$ zaroven, mozna pridam $u$ jako vrsek na konec), - sestrojime $P_e$ z $P_p$ (smazeme maxima pro smazane vrsky, pokud pribyl vsek $u$, pridame na konec hranu $e$, pokud hrana $e$ je tezsi nez nejaky prvek z $P_p$, prepisi na $e$ a to udelam skrz binarni vyhled predelu, kdy $e$ je uz lehci v $P_e$).| $n_i=$ \# vrcholu na $i$-te hladine ($n_i \leq n/2^i$), $c_i=$ \# porovnani pri prechodu z ($i+1$)-te na $i$-tou hladinu. Lemma: \# porovnani $\in O(n+q)$. Dk: $c_i$ chceme spocitat jako soucet cen vsech binarnich vyhledavani pri prechodu z $(i+1)$ na $i$tou hladinu, tedy pro kazdou hranu $e$ mezi $i$ a $i+1$ hladinami se rekurzime a binarne vyhledavame v $T_e$. $c_i \leq \Sigma_{e \in i+1, i} (1 + \log||T_e||) \leq$ ($||T_e||$ je normalne velikost, jenom pokud $T_e = \emptyset$ pak $||T_e||=1$, kvuli logu.) $\leq n_i + \Sigma{e \in (i+1),i} (L||T_e||) =$ (pomoci Jensenovy nerovnosti $f(x+y/2) > f(x)+f(y)/2$) a ze log je konkavni) $ = n_i + n_i \cdot (\Sigma_e(L||T_e||))/n_i \geq n_i \cdot log(\Sigma||T_e||/n_i)\leq n_i + n_i \cdot log((q+n)/n_i)$, pote spocitame $c_i$ pres vsechny hladiny...$\square$| Mejme algo, ktery overi zda je dana konstra minimalni a pokud $NE$, tak vyda lehke hrany ktere ji usvedcuji, tedy pro $\forall$ hrany, ktere nelezi v $T$ umi rict, zda je tezka/lehka a bezi v linearnim case. KARGER KLEIN TARJAN ALG: randomizovany algo, ktery bezi prumerne v linearnim case a najde mst, wc neni horsi jak boruvka. Myslenka: $e$ je tezka vzhledem k nejake $T$, pak $e$ nelezi ani v mst. Lemma: Necht $G$ je graf s vahami: $H\subset G$ vznikla nahodnym vzorkovanim hran s prsti $p$ (tzn. hodim si minci u kazde hrany, zda ji zahrnu), $F=MSF(H)$ tedy min kostrovy les, Pak $\mathbb{E}[\# e\in E(G): \text{$e$ neni F-tezka}] \leq V(G)/p$.(Jinymi slovy, pri vylepsenem Boruvkovi, kdy filtrujeme hrany, to znamena, ze prezije v prumeru nejvyse $V(G)/p$ hran). $\odot$ if $e$ not tezka, nemusi byt nutne lehka, muze byt v kostre, $\odot$ hrany spojujici dve komp $MSF(H)$ jsou povazovany za lehke. "Pokud si postavis kostru jen z nahodneho vzorku hran, tahle kostra bude fungovat jako skvely filtr. V prumeru zbyde v celem grafu jen velmi malo hran ($n/p$), ktere by tuto kostru dokazaly jeste vylepsi (lehke hrany)."
Dk: Zalozen na Kruskal algo, normalne bychom: vzali $G \rightarrow$ hodili minci pro $\forall$e $\rightarrow H \rightarrow$ setridili hrany v $H \rightarrow$ aplikovali Kruskal. Slejeme to dohromady (uz s prohozenymi kroky 3 a 4): 1. $F\leftarrow \emptyset$ 2. Setridime $\forall e$ v $G$ a pro ne: 3. if $F+e$ cyklus: zahodime $e$ 4. hodime minci $pr \leq 1-p$: zahodime 5. $e$ pridame do $F$. 3=$e$ je tezka, protoze $\forall$ hrany na cyklu uz jsou v $F$ a tedy tato je na cyklu nejtezsi, 4=$e$ je lehka, protoze bud budou jeji konc vrcholy spojeny jinou cestou, kde bude nejaka tezsi hrana (skrz setrideni) a nebo spojuje dve komponenty $F$, 5=$e$ neni tezka ani lehka. $\odot$ \# netezkych hran = \# hran, ktere 3krok nezahodil = \# hodu minci v 4kroku, $\odot$ po probehnuti $n$ hodu, kde padla hlava (tedy nezahazujeme) uz muzeme zbyte hran zahodit, protoze kostra je souvisla a tedy 3krok $\forall$ zbyle zahodi, $\Rightarrow \mathbb{E}[\#\text{netezkych hran}] \leq [\#\text{hodu v 4}] \leq [\# \text{hodu nez padne $n$-krat hlava}]$. Diky lemma o dzbanu: hlava padne s pr $p$, tak $\mathbb{E}[\#\text{hodu nez hodime hlavu}] = 1/p$ $\rightarrow \mathbb{E}[\# \text{hodu nez padne $n$-krat hlava}] = n\cdot\mathbb{E} = n/p$. $\square$ $KKT(G)$ ($G$ nemusi byt souvisly) (Input $G$ s vahami $w$ hran, Output $M = MSF \subset E$): 1. Smazeme izolovane vrcholy, if $G=\emptyset$ vratime $M=\emptyset$, 2. 2 Boruvkovy kroky na $G$ (2 faze kontrahujiciho Boruv algo) $\rightarrow$ mnozina $B$ zkontrahovanych hran (o $B$ vime, ze lezi v puvodni kostre $G$), 3. Vzorkujeme $H\subset G$ s $pr = 1/2$, 4. $F\leftarrow KKT(H)$, 5. $G'\leftarrow G$ bez $F$-tezkych hran, 6. $R \leftarrow KKT(G')$, 7. return $R\cup B$. Analyza $\odot$ zastavi se $\rightarrow$ pri rekurzi se pokazde v 2kroku snizuje \# vrcholu, nesnizil by pouze izol vrchy, ktere ale mazeme. $\odot$ najde min kostru $\rightarrow$ indukci, pokud $R$ je $MSF$ pak sjednoceni $R$ a $B$ dostaneme msf grafu $G$ pred kontrakci v 2kroku, a pokud $F$ je msf, tak pak muzeme smazat $F$-tezke hrany.| Slozitost:  Strm rekurze je bin, $v$ je vrchol rekurze, $G_v$ jeho podgraf, $m_v$, $n_v$, na $i$-te hladine $n_v \leq n/4^i$ (1krok pouze maze vrcholy, v 2kroku jeden krok Boruvky deli \# vrcholu 2), na $i$-te hladine $\leq 2^i$ podproblemu, protoze mame bin strom $\Rightarrow$ $\Sigma_{v\in i} n_v \leq n/2^i$ (pres hladinu $i$) $\Rightarrow$ $\Sigma_{\forall i}\Sigma_{v\in i} n_v \leq \Sigma_{\forall i} n/2^i = n \cdot\Sigma_{\forall i} (1/2^i)\leq 2n$ $\rightarrow \forall$uvahy muzeme delat na uplnem binarnim stromu o hloubkce $\lceil\log_4n\rceil$ (stromy rekurze budou vzdy jeho podgrafy) a strom rekurze ma hloubku $\leq \lceil L_4n\rceil$, protoze do synu vrcholu $v$ posilame max $n_v/4$ vrcholu. Lemma: V podproblemu $v$ travime cas $O(n_v + m_v)$ + rekurze volani. Dk: 1-urcite v $O(n_v)$, 2-prihradkove trideni, delam dva kroky - linearni, 3-vzorkovani linearni, 4-nepocitame, 5-prepoklad, ze je linearni, 6-nepocit, 7-linearni. $\square$ Lemma: wc slozitost kkt je $O(\text{min}(n^2, mLn))$. Dk: pro $O(n^2)$- $\Sigma_v O(n_v+m_v)= \Sigma O(n_v) + \Sigma O(m_v) = O(n) + \Sigma O(n_v^2) = n +(\Sigma n_v)^2 = n + n^2$ $=O(n^2)$, pro $O(mLn)$- podproblem $v$ a synove $l,p$, chci $m_l+m_p\leq m_v$ $\Rightarrow \Sigma_{v \in i} m_v \leq m$ a \# hladin $\leq L_4n \Rightarrow O(mLn)$; kolik hran se dostalo zaroven do $H$ a $G'$? ($m_l = m(H_v)$ a $m_p = m(G'_v)$) $\odot$ $m(H_v \cap G'_v) \leq |F|$, protoze to jsou hrany, tkere se dostaly do $H$ a pote nejsou vuci $F$ tezke, aby se dostaly do $G'$, ale to je presne kostra $F$ a navic $|F|\leq n_v/4$. $\forall$hrany padnou bud do $b_v$ (jsou Boruvkou zahozeny, nejdou ani do $m_l$, ani $m_p$), do $m_l$ nebo $m_p$ nebo $m_l\cap m_p$. $m_l +m_p+b_v = m_v+n_v/4$ (vpravo zapocitat prekryv dvakrat), zaroven $b_v \geq n_v/2$ (nejhur si v Boruv oba koncove vrcholy jedne hrany vyberou tu stejnou) $\Rightarrow m_l+m_p+n_v\leq m_v+n_v/4 \Rightarrow m_l+m_p+n_v/4 \leq m_v+n_v/4$ $\square$. Lemma $\mathbb{E}[\text{casu kkt algo}] \in O(n+m)$. Dk: Strom rekurze- nejdrive si najdeme leve a prave hrany. Leva cesta z $v$: $n_{v2} \leq n_v/4$, $m_{v2}\leq m_v/2$..$n_{vi}\leq n_v/4^i$ a $m_{vi}\leq m_v/2^i$, coz jsou geometricke rady a tedy $O(n_v+m_v)$. Chceme pocitat pres $\forall$ leve cesty: $\odot \forall$ leve csty zacinaji pod nejakou pravou hranou, krome prvni nejlevejsi. $\odot$ prava hrana $u\rightarrowtail v$: $n_v\leq n_u/4$ (Boruvka) a $m_v\leq n_u/2$ (Pouzijeme vzorkovaci lemma s $p=1/2$, protoze \# hran, ktere prezily je 2-krat \#vrcholu) $\rightarrow$ prumerny cas na levou cestu je $O(n_v+m_v)$ tedy $O(n_u/4 + n_u/2) = O(n_u)$
$\rightarrow \forall$ leve cesty uctujem rodicum $\rightarrow \forall$ rodici nauctujeme max 1 levou cestu $\rightarrow$ cas za $\forall$ leve csty je $\Sigma_{\forall v} n_v \leq 2n$ $\rightarrow O(n)$. A tan prvni nejlevejsi nas stoji $O(n+m)$. $\square$| WORD-RAM: slovo sirky $w\in \Omega(Ln)$, aritmetika v $O(1)$. POINTER MACHINE: dva typy objektu: symboly a pointery, symbol $\in \Sigma$ (konecna abeceda), ptr ukazuje na misto v pameti. Pamet: konstantne mnoho registru (registry pro symboly a pointery), alokovat libovolne mnoho krabicek (krabicka konstantne velka, cast na symboly, cast na ptry). Mame ptr na krab: cist z ni, zapisovat do ni, kopirovat mezi krabickou a registry. PM se ridi programem, pslpnst instrukci, instrukce umi pracovat s pameti a krabickami na nez mame v pameti ptr, umi ukladat konstanty do registru a kopirovat mezi registry/ registrem a krab. Na symbolech umime funkci arity $O(1)$, jejichz args jsou symboly a vysledek zase symbol. Ptry muzeme prirazovat, vytvorit krabicku a ulozit ptr do registru. Podminene skoky (podle symbolu a jejich funkci s $O(1)$ argumenty nebo porovnani pointeru), nepodminene skoky. $\odot$ neumime aritmetiku $O(1)$, pouze + a nasob v $O(n)$ a deleni v $O(nLn)$, $\odot$ lze PM prelozit do RAMU s konstantakrat zpomalenim, z RAM na PM zpomaleni delka slova-krat. VEKTORY RAM: $d$ slozek o $b$ bitech, jedno slovo $[x_2|0|x_1|0|x_0]$ ($x_i$ b bitu a separator), tedy $d(b+1) \leq w$. $M_{data}=$ maska, ktera ma 1 na datovych bitech a na separatorech 0, $M_{sep}=$ opak, $M_1=$ maska jednickoveho vektoru (coz je slovo $[0001|0|0001|0|0001]$). $Vytahnuti$ slozky: >> a $\&$ s bitovou maskou $0001..1$ ($b$ jednicek), $Prepsani$ slozky: $w$ \& $\neg$(jednic masky pro slozku $x_i$) a pak $or$ s daty posunute na pozici $x_i$. $Replikace(\alpha)$: chci vektor $(\alpha,\alpha,\alpha)$ ziskam pomoci $\alpha \cdot M_1$, (nasobeni bitu, ktere jsou daleko od sebe, funguje jak razitkovani). $Sum(X)$ proste udelam $X \cdot M_1$, z toho, jak funguje nasobeni pod sebou a pote posuneme >> o $(d-1)\cdot (b+1)$ bitu. $Compare(X, Y) = Z$: $Z=$(1/0 vektor: 1 kdyz $x_i<y_i$, 0 jinak), do vektoru $X$ si zapiseme 1 do separatoru $X' = X$ or $M_{sep}$, pote $R = X'-Y$ (pokud je $x_i \geq y_i$, tak si nemusime pujcovat ze separatorove 1, pokud $x_i<y_i$, tak se separatorova 1 zmeni na 0, protoze si z ni musime pujcit), nakonec extrakce a posun separatorovych bitu z $R$ na pozice slozek.| $Rank(X, \alpha) =$ \#i, ze $x_i < \alpha$ $\rightarrow$ $Sum(Compare(x, Replicate(\alpha)))$; $Min(X, Y)= Z$ (tedy $Z_i = \text{min}(x_i, y_i)$), potrebujeme spocitat masky $M_X$ a $M_Y$, ktere maji 1 na pozici bitu slozek, ktere jsou v $Z$, tedy $Z = (M_X \& X)$ or $(M_Y \& Y)$, $M_X: C=Compare(X,Y)\rightarrow$ mame 1 ve slozkach, kde je $x_i<y_i$, vezmeme $C'=C << b$, tedy vratime ty 1/0 zpatky do separatoru a pote $M_X=C'-C$. $Log(X)=$ zjisteni pozice nejlevejsi 1 jde taky v $O(1)$.| ZPET KOMOLOSZO (LCA V STRM): Chceme $\forall$ veci $Ln$ bitove (vektory budou $Ln$ bitu dlouhe), slovo je $Ln$ a slot $(1/3)Ln$. Lemma: Je-li $f$ funkce dvou argumentu velikosti slotu, lze v case $O(n)$ predpocitat table pro vyhodnoceni $f$ v $O(1)$. Dk: Slot ma $1/3Ln$ bitu, tedy $2^{(1/3Ln)}=n^{(1/3)}$ hodnot, tabulka muze mit $n^{(1/3)}\cdot n^{(1/3)} = n^{(2/3)} \in O(n)$ hodnot, vyhodnoceni $f$ trva polyn. v delce vstupu, coz je ale slot! tzn. $n^{(2/3)}\cdot poly(\log(n)) \in O(n)$, zbyva domyslet jak na RAM indexovat 2D pole v $O(1)$, jde!$\square$ KROK KOMOLA: rekurzime se na hranu $e$, prepocitat $T_e$, $P_e$ (v nem binarne vyhledame, od ktereho mista uz hrany mensi $\rightarrow$ stoji porovnani, prepsani zbytku $P_e$ uz stoji realie); prijdeme-li do vrcholu, vyhodnocujeme dotazy (vezmeme si vrsek z $T_e$ a podivame do $P_e$, tam je odpoved, stoji realie); zaplatime realie.| Pracujeme s vrcholy a hrany, co lezi pouze na ceste aktual vrchol-koren $\rightarrow$ strom je $O(Ln)$ melky, staci indexovat poradim na teto ceste $\rightarrow$ $LLn$ velke id pro vrchol, hrana = id spodniho vrcholu. $T_e$: bit maska, koduje mnz id vrcholu (bit = vrchol, 1 je vrsek nejakeho dotazu, 0 neni). $P_e$: dve repre! $Maly$ seznam: vektor id hran v jednom slotu (tretina slova, tedy do jednoho slotu cca $Ln/LLn$ prvku). $Velky$ sznm: $\Theta(LLn)$ slotu, reprezentuji jeden velky vektor indexovany hloubkou! (Kdyz vyhodim vrsek, nemusim po preindexovavat zbytek seznamu), vejde se to do $LLn$ slotu? hloubek je $Ln$ (\# polozek), $\forall$ polozky $LLn$ velke, vektor by mel merit $Ln \cdot LLn$ a 1 slot je $\Theta(Ln)$, takze v $\Theta(LLn)$ je dost mista. Velky szm kdyz $|T_e| \in \Omega(Ln/LLn)$ (velikostslotu / velikostpolozky).
V te chvili bin vyhledavani v $P_e$ stoji $L|T_e| \in \Omega(L(Ln/LLn)) \in \Omega(LLn - LLLn) \in \Omega(LLn)$, proto kdyz jsem donucen pouzit velky szm, tak konstant \# operaci s velkym szm, ktere budou travit konst cas s kazdy slotem se zaplati tim bin. vyhledavanim v $P_e$. Pozor! nesmime pracovat s velk szm kdyz pro novou hranu $e$ je $T_e$ uz maly, ale na hrane nad nami je velky $\rightarrow$ zavedeme ptry, v malych szmech a budou se  odkazovat do nejblizsiho nadrazeneho velkeho seznamu. Jak se v celem algo skrz dfs rekurzime, tak si predavame info jaky je nejblizsi nadrazeny velky sznm, tedy vuci cemu dereferencovat.| Operace: $Weight(X)$ = \# 1 bitu v cisle $X$ (pokud $X$ velikost slotu, tak tabulkou (druhy arg ignorujeme), pokud vel slova, tak pro $\forall$ tri sloty tabulkou), $FindKth(X, k)=$ pozici $k$-te 1 v $X$ od spoda ($X\in$ |slot|$\rightarrow$ lemma, if $X\in$ |slova| $Weight$ pro $\forall$ 3 sloty $Weight$, zjistim do ktereho sltu $k$-ta 1 a tam $FindK$), $Bits(m)=$ maly seznam pozic 1 v $m$, $Select(X,m)=$ posklada $\forall$ vybrane bity maskou $m$ z cisla $x$ za sebe, $SubList(x,m)$ = (x je maly sznm) vezme prvky vybrane maskou $m$ (1 na pozici $i$ v masce $\rightarrow$ prvek $i$ ano) a sklepe je za sebe.| Dopaddingivovame maly seznam do |slotu|, pro prazdny same 0 (indexujeme hloubky od 1). 
OPAKOVANI: slova ($Ln$ bitu) / sloty ($1/3Ln$), $id$ vrcholu i hrany ($LLn$ bitu), $T_e$ (vrsky) bit maska $Ln$ bitu, $P_e$ (maxima) - maly sznm= 1 slot (polozka $LLn$ bitu) - velky sznm= $LLn$ slotu + behem dfs si predavame ptr na nejblizsi velky sznm. Lemma: V ccase $O(n+q)$ lze spocitat hloubky a vsechny $T_e$. Dk: hlbka trivialni (staci +1) k aktual $h$, $T_e$ pocitame pri vraceni dfs (vracime z $v$ pres hranu $P$ a $e_1\_e_k$ hrany do synu z $v$ ) $\rightarrow$ zoruju: $T_P=$$T_{e_1}$ or $T_{e_2} \_ T_{e_k}$, lebo $\forall$ vrsky cest,k tere sly pres $e_1..e_k$ jsou i pro $P$, krome $v$ $\rightarrow$ vyhodim $v$ a nakonec pridam $\forall$ vrsky dotazu, ktere konci ve $v$. $O(\#\text{hran z} v + \#\text{dotazu})$ = $O(n+q)\square$. (v $P_e$ pro bin vyhled musime umet: maximum na $i$-te pozici + jaky je $i$-ty vrsek v poradi aktiv. vrsku) Lemma: $T_e$ a $P_e$ lze indexovat v $O(1)$. Dk: u $T_e$ vlastne $i$-ty 1 bit zleva $\rightarrow$ lemma o f s 2 slot args $\rightarrow$ zjistim $h$ vrcholu a behem dfs udrzuji zasobnik, ktery tou hloubkou indexuji; $P_e$ if maly sznm $\rightarrow$ indexovan jiz poradim $T_e$ $\rightarrow$ bit posuny (+check if not pointer, jinak index velky sznm); if velky sznm $\rightarrow$ zjistim hloubku (z $T_e$) = pozice ve velkem seznamu$\square$ (vstoupime do $v$ po hrane $P$ a musime odpovidat na dotaz $q = (t, v)$ $\rightarrow$ vytahnout pomoci vrcholu $t$ z $P_p$ cestove max) Lemma: $P_e$ lze indexovat vrcholem v $O(1)$. Dk: vrchol na hloubku, $P_e$ je velky pak indexujeme hloubkou, $P_e$ je maly $\rightarrow$ need kolikaty vrsek je to v bit masce aktiv vrsku pro $e$ $\rightarrow$ "kolik je 1 od zacatku doprava" $\rightarrow$ $T_e$ or $M$, kde $M$ maska plna 1 po hloubku vrcholu pak jen funkce \# 1 v cisle$\square$ Komolosz algo: 1. odpovidame na dotazy se spodkem $v$. $\forall$ e: 2. $T_e$ (mame z predvypoctu) 3. $P_e$ - zdedime z $P_P$ (zde KONVERZE SEZNAMU) - pridame $e$, if maly pridame polozku an konec (<< a or), if velky pripiseme do prislusne polozky dle aktual vrcholu - binarni vyhled (pouzijeme indexovani v $O(1)$) - prepsani mensich polozek, if maly tak bitova maska a zkopirovani hodnoty kterou chceme, if velky prepsani trva $\Theta(LLn)$, ale to i bin vyhled, proto ok. KONVERZE SEZNAMU: 1VtoV: pouze cp polozek $O(LLn)$ muzeme diky bin vyhled. 2MtoM: masky $T_p$ $T_e$ (o nejake vrsky jsme prisli, pridani $v$ jsme vyresili po konverzi!) $X = Select(T_e, T_P) \rightarrow Select(P_p, X) = P_e$. 3VtoM: mame konstant mnoho casu (beznadej), ale pouzijeme $Bits(T_e)$ $\rightarrow$ maly seznam hloubek aktiv vrsku, coz jsou ptry do toho velkeho. 4MtoV: prvni dereferenc ptry  v malem $\rightarrow$ najdem usek ptru v malem, ktere ukazuji do jednoho slotu ve velkem $\rightarrow$ polozky v malem vypadaji [|1|ptr] nebo [0|hloubka] tedy pomoci dvou $Compare$ s horni a dolni mezi (nareplikovanou do vektoru) intervalu dostaneme masku polozek, ktere jsou ptry a jsou v intervalu, s tou maskou vynulujeme ostatni, pote mame slozku z velkeho a slozku s pouze ptry co nas zajimaji $\rightarrow$ f o dvou args v $O(1)$, kterou si predpocitam tak, aby ignorovala offset slotu ve velkem seznamu (ptr je totiz (slot, pozice ve slotu)); pote rozstrkat podle hloubek do velkeho (stihnout v $O(LLn)$), 1 slot ve velkem je interval hloubky $\rightarrow$ odpovida konstantne velkemu useku $U$ v $T_P$ a tomu nekonstant velka cast v $P_P$, v $O(1)$ urcime hranice $x,y$ teto casti v $P_P$ (na $T_e$ $y$="\# 1 bitu za koncem $U$" a $x$=".. od zacatku $U$"), zkopirujeme z useku $(x,y)$ v $P_P$ polozky do slotu ve velkem $\rightarrow$ f o dvou args, kde oba $\leq$ |slot| $\in O(1)$, opakujeme pro $\forall$ sloty $\rightarrow O(LLn) \cdot O(1)$.$\square$ CELA VERIFIKACE: 1. Sestrojime boruvkuv strom na kostre - $O(n)$ 2. Stipeme cesty pricne na 2 svisle (ds na $LCA$, pracuje dost rychle) 3. Pustime tohle v $O(n+q)$ (na RAMU umime najit $\forall$ hrany, ktere jsou vuci kostre lehke v $O(n+q)$, coz treba pouzival randomizovany algo). PARALEL HRAN PM: V kontrahujicim Boruvko jsme museli odstranovat paralelni hrany a nechat tu nejlehci (na ram prihradkove - $n$ polozek rozsahu $R$ trva $O(n+R)$): Orientovana hrana = ($v$, $u$, $id$). Repr $G$: spojak vrcholu(seznam vsech vrcholu), $\forall$ vrchol=krabicka, kde je ptr na spojak ptru na vrcholy (odchozi hrany), $\forall$ $v$ si vyrobim prihradku (spojak), pak budu prochazet vrchol $v$ a jeho spojak hran $(v,u,id)$, a tu hranu zapisu do $u.Prihradky$ $\in O(1)\cdot O(n+m)$, pote projdu tyto prihradky a identifikuji paralelni hrany. Pro Neorientovane hrany- pridame i ptr z $u$ do $v$ (oba vrcholy o sobe vi), jakmile poprve narazime na hranu a jeste neni navstivena, tak ji pridame do prihradky toho koncoveho vrcholu a pote u ni zaznamename, ze uz je navstivena. ISOMORF STROMU: Zakoreneny ciste bin. strom ($\forall$ vnitrni vrchol ma 2 deti) - prvni BFS, identifikovat listy, udelam si $id$ tridy izomorfismu (jen ptr nekam), listy dostanou vsechny stejny $id_0$, jdeme po hladinach $\forall$ isomorfni stromy na stejne hladine, kazdy vrchol popiseme dvojici $id$ synu: $(id_l, id_p)$ x x x x x x x 
% TODO - NECHAPUUUU
prednaska 6: 1:23:00 - konec. | Co slozitost? $\rightarrow$ muze byt az kvadraticka! pri kazdem prihradkovem trideni musime prochazet $\forall$ prihradky a tech je moc, budeme udrzovat seznam pouzivanych prihrade $\rightarrow$ to rozbije trideni, ale da stejne dvojice ($k$-tice) do stejnych prihradek, to nam staci! Pro Nezakorenene: df: $Excentricita$ $e(v) = \text{max}\{d(v,w)|w\in V\}$, $centrum$ grafu je mn vrcholu grafu, pro ktere $e(v)$ minimalni. Pro strom staci oloupavat vzdy $\forall$ listy $\rightarrow \forall$ excentricity klesnou o 1 $\rightarrow$ dostaneme bud centrum (1 vrchol) a zakorenime v nem nebo bi-centrum (muzeme podrozdelit a zakorenit v nem) $\Rightarrow$ Izomorf na PM v $O(n)$. UNIFIKACE RETEZCU: Vstup retezce $S_1\_S_n$ slozene ze znaku a ptru (jen predpoklad, ze pokud ukazuje ptr na nejakou krabicku, tak v ni mame konstantni vyhrazene misto), Vystup tridy rovnosti (pouze seznamy $id$ retezcu, nekopiruji se cele), v case $O(n + \Sigma_i |S_i|)$: 1. roztridime retezce podle delek (dva prsty, jeden na znacich retezce druhy na meritku, pote priradim do seznamu u daneho meritka) 2. pro kazdou delku tridime prihradkove s udrzovanim aktivnich prihradek. GRAF VYPOCET: Vstup: $G$ + ohodnoceni (symboly $\in \Sigma$) vrcholu a hran, Vystup: jine ohodnoceni (symboly $\Sigma$, ptry na $v\in V$, $e\in E$ a externi ptry). LCA se vejde do Graf Vypoctu: vstup $G$ s dvema typy hran (puvodni a dotazy), vystup bude graf s ohodnocenim dotazovych hran nejakym vrcholem. (predpokladame, ze kazdy typ dotazu 1x, pokud ne, prvni pustim unifikaci na dotazy). Df: Grafovy vypocet je topoologicky $\equiv$ respektuje izomorfismus (jeden konkretni izo!, ktery zobrazi vstup na jeden vstup, tak ten stejny zobrazi vystup na vystup.) napr. u nejkratsi cesty (tedy ohodnotime hrany, ktere v ni jsou) to neplati pro obecny izo, protoze muzeme mit vice nejkratsich cest a tedy dva vystupy nemusi byt obecne izomorfni.; Df: $Kanonicky kod$ ohodnocenoeho grafu: dfs a pri nem prirazujeme $ID(v)$ (tak jak je postupe objevujeme, opet pomoci metru), $kod(v) = vlabel(v)[ID(w)elabel(v,w)][kod(w)elabel(vw)]\otimes$ (1. "[]" opakujeme pro $\forall vw$ zpetne hrany) a (2. "[]" pro $\forall$ stromove hrany). Dekodovani: zpetna hrana zacina $ID(w)$, coz si kodovaci alg tvori sam, je to tedy jine nez cokoliv na vstupu; cast pro strom hranu zacina barvickou $vlabel(w)$ a $id$ od barvikcy urcite umim poznat $\rightarrow$ kodovani i dekod v $O(n+m)$. Delka kodu: zpetna hran= 2 znaky, stromova= 1z (tedy hrana$\leq$ 2z), vrchol= 2z $\rightarrow$ $\leq 2n + 2m$, ($m \leq n\cdot(n-1)/2 = (n^2-n)/2)$, $\leq 2n + 2(n^2-n)/2 = n^2 + n = n\cdot(n+1)$. Df: Grafy jsou $kanonicky ekvi$, pokud maji stejny kanonicky kod. Df: $Kolekce$ grafu $\mathcal{C}$, $|\mathcal{C}| =$\# grafu v kolekci, $||\mathcal{C}||= \Sigma_{G\in\mathcal{C}}(n(G) + m(G))$ (velikost vstupu v \# pointeru). Thm: Mejme topolog vypocet $\Gamma$ (mame tedy algo, ktery ho umi), ktery pro graf na $l$ vrcholech lze v case $T(l)$, pak lze $\Gamma$ vyhodnotit na kolekci $\mathcal{C}$ grafu s $k$ vrcholy v case $O(||\mathcal{C}|| + (k+s)^{k(k+1)}\cdot (T(k) + k^2))$. ($s$= konstanta zavisla na $\Gamma$), ($||\mathcal{C}||$= velikost vstupu, umoznuje zakod kazdy graf na vstupu nebo zkopirovat vstup), (2.clen= horni odhad na \# kanon kodu grafu na $k$ vrcholech: kod je pslpnst delky $\leq k(k+1)$ a znaky pslpnsti jsou bud $id$, tech je $\leq k$ a nebo barvicky, ci $\otimes$ a to se vleze do $s$; pozor, musime kratsi kody doppadingovat $\otimes$ az do konce.) (2.clen je zaroven \# moznych kanon ekviv trid a na tolik max grafu budu spoustet nas algo proto $\cdot T(k)$, pro ostatni grafy jen recykluji vysledek) (do $k^2$ schovam kodovani/dekodovani pro $\forall$ kanon ekv tridy, autori puvodne tvorily vsechny kanon ekv tridy, coz mi nedelame diky unifikaci, takze muzeme skrtnout).($\odot$ dekodovani lze udelat tak, aby odmitlo nekorektni kod) Jak to bude fungovat: 1. zakodujeme grafy z $\mathcal{C}$ (se vejde do $||\mathcal{C}||$) (nazyvame konkretni grafy) 2. unifikace kodu (kazdy kod grafu velky v $O(\text{velikost grafu})$ takze vsechny v  $O(||\mathcal{C}||)$ a tedy unifikace dobehne v linear case) $\rightarrow$ tridy kanon ekviv, 3. $\forall$ tridu: -sestrojime genericky graf (dekodujeme jeden kod) -spustime na nej alg z predpokladu a tim zname pro $\forall$ kanon tridy vystup pro jejiho reprezentanta, 4. rozkopirujeme genericke vystupy ke konkretnim grafum: -dfs na reprezentanta tridy a na konkretni graf, pro ktery chcem vystup zkopirovat, - behem toho vytvarime prislusny izo (na PM to znamena priradit ptr ve vrcholu nebo hrane ptr na vrch/hranu v tom konkretnim G, na ktery se vrch/hrana zobrazi), -druhym pruchodm tim zkopirujeme do konkretniho grafu: symboly, odkazy na V/E, externi ptry; - !kdybychom kopirovali rovnou, byl by problem s "ptry na V,E", protoze po zkopirovani tohoto ptru do konkretniho grafu by ukazoval na v/e do reprezentanta.| ACKERMAN: Df: $A: \mathbb{N}\rightarrow\mathbb{N}$: $A(0,y) = 2y$, $A(x,0) = 0$, $A(x,1)=2$ pro $x\geq1$, $A(x,y) = A(x-1, A(x,y-1))$ pro $x\geq1$ a $y\geq2$. 1.radek $=2y$, 2.radek $2^y$, 3. radek $2\uparrow y$, $A(n)=A(n,n)$, $\odot$ radky a sloupce $\geq 3$ rostou. Df: $radkove$ inverze: $\lambda_i(n)= \text{min}\{y | A(i,y) > \log n\}$, $\lambda_0(n)\in O(Ln)$ "kdy $2y$ preroste $Ln$ .. = $1/2 Ln$", $\lambda_1(n)\in O(LLn)$, $\lambda_2(n)\in O(L^*Ln) = O(L^*n)$, diagonalni inverze: $\alpha(n) = \text{min}\{ i | A(i,i) > Ln\}$, $\odot$ ze vsech realistickych cisel je $\alpha \leq 4$. Pro $m\geq n$: $\alpha(m,n) = \text{min}\{x\geq1 | A(x, 4\lceil m/n\rceil)>Ln\}$ - $\odot$ vlastne inverz sloupecku, ale to jaky sloupecek si vyberu je dano $4\lceil m/n\rceil$, pro ridke grafy roste rychle, pro huste si vyberu sloupec vic vpravo a roste pomaleji. $\alpha(m,n)$ se maximalizuje pro $m=n$, $\alpha(m,n) \leq 3$ pro $Ln<A(3,4)$ (pro $\forall$ efektrivne mala $n$ je $\alpha(m,n)$ omezena konstantou), $\alpha(m,n)\leq\alpha(n) +1$, $A(x,4\lceil m/n\rceil) \geq A(x,4)$ (kazdy radek roste) $=A(x-1, A(x,3)) \geq A(x-1, x-1)$ (protoze $A(x,3)\geq x-1$) (tedy pokud pujdeme po diagonale, doku $Ln$ neni vetsi jak $A(x-1,x-1)$, tak pak uz je urcite vetsi jak $A(x, 4m/n) \Rightarrow \alpha(m,n) \leq \alpha(n)+1$), pro pevne $i$ a $m\geq n\times\lambda_i(n)$ je $\alpha(m,n) \leq i$ (-kdykoliv hustota grafu je aspon $\lambda_i(n)$, tak $\alpha(m,n)$ uz je nejvys $i$ - if budeme vyrabet $G$ s hustotou aspon $L^*n$, tak vime, ze $\alpha(m,n)\leq 3$), $\Rightarrow A(x, 4\rceil m/n \lceil) \geq A(x, \lambda_i(n)) \geq Ln$ (pro $x\geq i$, plyne z definice $\lambda$).
Union-Find: $n$ prvku na nich $m$ operaci $\rightarrow \in O(m\cdot \alpha(m,n) +n)$. OFFLINE LCA (pomoci U-F) (linearni): sikovne dfs: prochazime stromem, tvorime u-f ekviv tridy pro kazdy vrchol $u$ na ceste koren-aktual vrchol ($v$) a levy podstrom $u$ je v jedne ekv tride s $u$ (tedy co jsme uz prosli). Jakmile vystupujeme z $v$, vyhodnocujeme dotazy napr $(v, x)$, ktere vidime uz podruhe a tedy vedou do jiz zpracovane casti, pak $LCA = Find(v,x)$, coz je vrchol na ceste z korene do $v$. $q \cdot Find + n\cdot Union \rightarrow$ cas $O((n+q)\alpha(n+q,n)+n)$. Linearizace casu: $\mu$-strom: $v$ je koren $\mu$-stromu $\equiv$ $(|T(v)|\leq g \& |T(parent(v))|> g)$. Smazeme-li $\mu$-stromy vcetne hran za ktere visi, dostaneme $M$-strom. $\odot \forall$ list $M$-stromu ma pod sebou aspon jeden $\mu$-strom; if vrchol $v$ $M$-stromu ma syna koren $\mu$-stromu, tak $v$ nemusi byt list $\Rightarrow$ \# listu $M$-stromu $\leq n/g$.| If dotaz $q$ cely v $\mu$-strmu $\rightarrow$ vyridim v nem, if $q$ cely v $M$-stromu $\rightarrow$ vyridi $M$-strom, if $q$ mezi dvema $\mu \rightarrow$ prevedeme na lca listu $M$-stromu, if $q$ mezi $M$ a $\mu \rightarrow$ lca vrcholu v $M$ a listu v $M$ podkterym je koren $\mu$. lca v $T$ $\rightarrow$ lca v $M$ nebo $\mu$ stromu.| Vypocet $\mu$-$M$ dekompozice: poridime "metr" dlouhy $n$, pri pruchodu dfs posouvam ptr $p_1$ na metru a druhy $p_2=p_1+g$ paralelne; jdu do vrcholu $v$, posunu o 1 ptry, do krabicky $p_2$ pridam $v$-zarazku, a oznacim $\forall$ zarazky ve $v$ za oznacene; jakmile vylezam z $v$, podivam se na jeho zarazku, pokud oznacena, tak $\in M$-strom pokud ne $\in \mu$-strom (navic koreny $\mu$-strom jsou ty, jejichz zarazka nenarazila, ale rodice uz ano). Zarazek vyrobim $O(n)$ a s kazdou travim $O(1)$ casu $\rightarrow O(n)$ + behem dfs zvladneme pridat do vsech vrcholu $\mu$-stromu ptr na list $M$-strmu pod kterym $\mu$ lezi. Poznam zda je $q$ v jednom $\mu$ $\rightarrow$ porovnam prislusne listy v $M$-strmu.| Dotazy v $M$: special u-f: $n$ prvku, $c$ cervenych, $m$ operaci a $\forall$ $Union$ se ucastni aspon 1 cerveny prvek $\rightarrow O((n+q)\cdot\alpha(n+q, c)+n)$. Necervene prvky: bud ve vlastni ekv tride, nebo se jiz ucastnil $Union$ a tedy je v ekv tride s cervenym. $\forall$ ekv trid si pamatuji cerveneho repre, $\forall$ necervene si pamatuji, ke kteremu cervenemu prvku patri, tedy ekv tridy na cervenych prvcich a navic kazda takova ma jeste sveho repre. $Union$ 2 cerven: un na cervenych tridach, $Union$ 1 cervene a 1 necerveneho prvku: povesime necerveny prvek pod repre te cervene. $Find$ 1 necerveny 2. jakykoliv $\rightarrow$ vzdy ne, 2 necervene prevedeme na 2 cervene pod kterymi jsou poveseni, $Find$ dvou cerven, normal. Necervene prvky stoji $O(1)$ navic a vse ostatni bezi na cervenych. Prohlasime listy $M$-stromu za cervene. $Union$ delame zespodu nahoru $\rightarrow$ vsechny obsahuji aspon 1 cerveny prvek $\Rightarrow$ pro $M$-strm to bezi v $O((n+q)\alpha(n+q, n/g) +n+q)$, lebo $c\leq n/g$ a pro $g \geq \lambda_i(n)$, tak to je $O(n+q)$, lebo $(n+q)/(n/g)\geq n/(n/g) = g \geq \lambda_i(n)$. Muzeme i omezit $g\leq L^*n$.| Pro $\mu$-strom: pouzijeme topolog vypocty: vstup= obarvime vrcholy a hrany: vrchol, koren, vrchol s dotazem, hrana, hrana dotazu. $|V|\leq g$. Imple detail: dotazy vickrat - prihradkove. Vystup = hrane dotazu dame ptr na vrchol (lca). Dosadime: $O( ||\mathcal{C}|| + (g+s)^{g(g+1)} \cdot T(g))$, 1. clen urcite $O(n+q)$, 2. clen (pro dost velke $g$) $\leq (2g)^{(g \cdot 2g)} \cdot T(g)$ $\leq (2g)^{2g\cdot g + c}$ $= 2^{(1+Lg)\cdot(2gg +c)} \in o(g^3)$ $\Rightarrow$ if zvolime $g=L^{(1/3)}n$ ($g\geq \lambda_in$ ano $\rightarrow$ $M$-uroven projde), lebo $2^{o(ggg)} = 2^{o(Ln)} \in o(n)$. VERIF MST (ktere hrany jsou lehke?): stejna dekompozice, chceme pro $\forall$ hranu $\not\in T$ if je lehka, if dotaz pouze v $M$/$\mu$, tak odpovida samotny $M$/$\mu$ strom. If dotaz mezi dvema $\mu$-stromy, tak nahradim 2$\mu$-dotazy + 2 trivial (hrana spojujici koren $\mu$ a list $M$) + 1 $M$-dotaz, kazdemu dotazu dam vahu puvodniho dotazu (hrany) a pokud je aspon jeden novy dotaz lehky, pak i puvodni dotaz hrana lehka. (Reprezentace vah na PM: cerna skrinka $O(1)$, v realu prenasobime slozitost algo slozitosti cerne skrinky); $M$-uroven: pomoci Link-Eval: les zakorenenych stromu, ohodnocene hrany, operace: $Link(u,v,\gamma)$ - prida hranu z korene $u$ do nejakoho vrcholu $v$ s ohodnocenim $\gamma$; $Eval(u)$ - max na ceste $u$-koren. l-e se da implementovat pomoci u-f a $\forall$ l/e vyuzije amort konstantne operaci u/f $\rightarrow$ cas ($m$ operaci, $n$ prvku) opet $O(m\alpha(m,n)+n)$. Jako v lca stavime l-e les zespodu nahoru pri dfs. Pri vraceni z $v$ do $u$ udelame $Link(v,u,\gamma)$ a $\gamma$= vaha strom hrany. Dotaz $uv$ vyhodnocujeme v tom, vrcholu ktery je vic nahore $v$ a az kdyz se z nej vracime, druhy vrchol $u$ je v podstromu $\rightarrow Eval(u)$ na podstromu $v$ a porovname s vahou dotaz hrany. Za $\forall$ hrany stromu $Link$, za $\forall$ hrany dotazu $Eval$. $Link$ opet ze spoda nahoru $\rightarrow$ trik s cervenymi prvky $\rightarrow$ pro $g \geq \lambda_i(n)$ opet linearni. 
$\mu$-uroven - problem hledani lehkych hran nejde popsat topolog vypoctem (nemame jak popsat vahy) $\rightarrow$ z Komolosz algo plyne, ze $\exists$ rozhodovaci bin strom, v uzlu porovnavame vahy dvou hran, list= skoncili jsme vypocet a tohle jsou lehke hrany, z komolosz je hloubka tedy $O(n+q) \in O(g^2)$. (jsou to tedy $\forall$ pruchody komolosze na $\mu$-stromu). High-lvl: $\forall \mu$-stromy si topolog masinerii rozdelime na kanon ekviv tridy, pro kazdeho reprezentanta sestrojime rozhodovaci strom a pak jej spustime zpatky na $\forall \mu$ stromy v dane ekv. tride uz s jejich vahami. Jak rychle vyrobime rozhod strm?: \# lin. usporadani na hranach $\leq (g^2)! \leq g^{2gg}$ pro $\forall$ z nich pustime komolosze, ten vygeneruje jednu cestu v rozhodovacim strme $\rightarrow$ do listu vypiseme lehke hrany pro dane lin usporad na vstupu. Komolosz je na ram $O(n+q)$ $\rightarrow$ prevadet z pm na ram za cenu kvadratic zpomalnei $\rightarrow$ komolosz na pm v cas $\leq g^c$ $\Rightarrow$ rozhod strom umim vyrobit v cas $\leq (\#\text{lin usporad})\cdot(\text{komolos na pm})$ $\leq g^{2gg}\cdot g^c = 2^{(Lg \cdot2gg)} \cdot 2^{(Lg \cdot c)} = 2^{(Lg \cdot(2gg+c))}$ pro dost velke $g$ je to $\leq 2^{ggg}$. Dosadime do $O(||\mathcal{C}|| + (g+s) \cdot T(g))$ (opet $\mathcal{C} \in O(n+q)$) $\rightarrow \in O((2g)^{2gg} \cdot 2^{ggg})$ (upravime 1. clen: $(2g)^{2gg} = (2\cdot2^{Lg})^{2gg} = 2^{(1+Lg)\cdot2gg}\leq2^{ggg}$) cele tedy $\leq 2^{2ggg}$. $\Rightarrow g=(1/2)L^{(1/3)}n$ pak opet $O(2^{2ggg})$ bude v $O(n)$ a taky je stale $g$ dost velke, aby $\alpha$ byla konstanta. Technic detail: Nemuzeme odkazovat primo z exter objetu, tedy rozhod stromu, na hrany do grafu, ktere porovnavame: mezikus, do ktereho vedou ptry pro vrchol rozhod stromu na dve hrany, ktere porovnavame a z tech konkret grafu dame ptry hranam do prislusneho policka mezikusu + 1 ptr z korene na koren prislusneho rozhod stromu; pote pouze projdeme $\mu$-strmem a nahazime vahy hran do danych policek mezikusu a pak projdeme rozhod stromem.
LINK-EVAL: les zakorenenych stromu orientovanych ke koreni (na zacatku jsou $\forall$ 1 vrcholove), ohodnocene vrcholy $l(v)$ (nebo hrany) prvky pologrupy $(S, \odot)$ (nosna mn $S$ a 1 asociativni binarni operaci), operace $Eval(u)$ - vysledek operace $\odot$ aplikovane pres cestu $u$-koren (znacime $e(u)$), $Link(u,v)$ - prida hranu $u\leftarrowtail v$ mezi koreny, $Update(u, \alpha)$ - $l(u) = \alpha$ pro koren $u$. Muzeme implementovat trivialne, ale zrychleni. U-F vypada tak, ze mame les stromecku, stromecek orient do korene, dve optimalizace: Union spojujici koreny 2 stromecku pripojuje mensi pod vetsi (bud by size nebo by rank (kdy zvysim rank novemu celkovemu koreni, pokud pripojuji dva koreny stejnych ranku)) zarucuje hloubku $O(Ln)$, komprese cest (ptr na koren pro vrcholy na prochazene ceste, uamortizuje se na Find v $O(Ln)$) a kombinaci techto dvou dostaneme amort $O(\alpha(m,n))$. Kompresy cest muzeme delat i u L-E! Vrcholu dame vysledek operace $\odot$ vsech vrcholu na casti cesty, kterou jsme tim zkratili (zvladneme v $O(\text{delka cesty})$ a nezmeni vysledek $Eval$), if pouzit only kompres $\rightarrow$ dosta na slozitost $O(Ln)$. Kompresi vytvorime novy virtual strom $V$ ze skutec strmu $T$ a plati invarianty: 1. $V$ a $T$ maji stejne vrcholy, 2. $V$ a $T$ maji stejny koren a jeho label 3. $V$ a $T$ se shodnou na vysledku $\forall$ $e(\_)$. VAR1: operace $\odot$ ma inverzi (levostrannou), musime zahodit invariant2 (virtual i skutec si pamatuji jejich koreny). Mejme skutecne stromy: $r$ koren $T$, $s$ koren $T'$, delame $Link(r,s)$ a chceme $\forall x \in T': e'(x) = e(x) \odot l(r)$, v reci virtual strmu: $u$ koren $V$ a $r\in V$ (odpovida $T$), $v$ koren $V'$ a $s \in V'$, pokud nejsou $r$, $s$ deti korene, tak na ne provedu kompresi, aby byly. Kdy $Union$ by size rekne pripoj $V'$ pod $V$ ($u\leftarrowtail v$), tak super, to odpovida skutec stromum: ve virtual musime jeste zaridit: do $eval$ v $V'$ byl zapocitan $e(r) = l(r) \odot l(u)$ (protoze uz $r$ je syn $u$), udelame novy label $l'(v) \leftarrow l(v)\odot l(r)$. If $r=u$, pak $e(r)=l(r)$ a if $s=v$ tak se nezmeni nic. If union by size rekne $V$ pod $V'$: tak musime prelabelovanim $v$ zaridit to, aby $Eval$ vychazel spravne: $l'(v) \leftarrow l(v)\odot l(r) \odot l(u)$, tim ve $V'$ vychazi vsechny $Evaly$ spravne, ale rozbil jsem si eva v $V$, lebo prochazi pres $v$, chceme: $l'(u) \odot l'(v) = l(u)$ $\Rightarrow$ $l'(u) \odot l(v) \odot l(r) \odot l(u) = l(u)$ $\Rightarrow$ $l'(u) \leftarrow (l(v)\odot l(r))^{-1}$. VAR2: $S$ je linear usporad a $\odot$ je max (/min), pridam invari4: virt strom je slozeny z podstromu $V_0, V_1\_ V_k$ s koreny $r_0\_r_k$, $\exists$ hrany $r_{i+1} \rightarrowtail r_{i}$ a $l(r_{i+1}) \geq l(r_i)$. Delam-li $Eval(x)$ pro $x\in V_i$, tak zalezi jen na vrcholech ve $V_i$, $Eval$ se deje jen ve $V_i$. $Update(r_0, \alpha)$ - if novy $l'(r_0) \leq l(r_1)$ hotovo, jinak $\exists$ usek $\forall i\in [1,j]$: $l(r_i)<l'(r_0)$ (koreny $r_i$ v obousmernem spojaku, abychom tento usek nasli) a slevame $r_1\_r_j$ pomoci $Union$ by size ($U(U(r_1,r_2),r_3)$..) a $r_0$ se neucastni, aby se zachoval inv2; $size(r_i) = \Sigma_{j\geq i} |V_i|$, if chci pouze $|V_i|$, staci odecist $size(r_{i+1})-size(r_i)$. $Link(r,s)$: muzu beztrestne $Update(s, \text{max}((l(s),l(r)))) \Rightarrow l(s)\geq l(r)$ a mam $r_0=r, r_1,\_,r_k$ a $s_0=s, s_1,\_,s_t$,pak pripady A-$size(s)\leq size(r)$: tak zrusim $s_{i+1}\rightarrowtail s_i$ a napojim $\forall$ $s_i \rightarrowtail r_0$ (nemusim prelabelovat); B-$size(s)>size(r)$: tak PREpojim $\forall r_i |i\not=0$ na $r_i\rightarrowtail r_0$ misto $r_{i-1}$ a $s=s_0 \rightarrowtail r_0$, evaly v podstromu celem $s$ vyjdou, a opet stejny argument pro $r_i$ a na hrane $s_0 \rightarrowtail r_0$ je spravna nerovnost.| $\forall$ vyrabeni a slucovani se uamortizuje, $\Phi$ je \# podstromu (\# hran mezi koreny(cervene hrany)), $Eval$ na $Phi$ nesaha. Zjistit if $size(s)\leq size(r)$ v $O(1)$, lebo $size$ si pamatujeme a umime $O(1)$ prepocitavat $\rightarrow$ pripad A: slucujeme podstromy, ale potencial klesne o \# prepojenych podstromu, zaplati se to; B: opet prepojeni $r_i$ zaplatim z potencialu a pak zvysim o 1 celkovy \# podstromu a to nauctuji operaci.| Lemma(vyvazenost): $\exists a>1, c>0$: \#vrcholu vysky $h$ $\leq (cn)/a^h$, pak kdyz delame union by size/rank, tak vyjde slozitost $O(\alpha(m,n))$ (pokud by platilo i pro $Linky$, tak stejna slozitost)| Mam-li "vyvazeny" strom a provadim pslpnst komprese cest, tak ty komprese trvaji amort $O(\alpha(m,n))$, ale pro vyvazenost uvazuji v $U$= les vsech $V_i$ (virtualni) bez kompresi (komprese nezmeni rozhodnuti $Unionu$ by cokoliv, protoze nesaha na koren) a $s(v)$= \# potomku v $U$ (tedy \# potomku v ramci jednoho podstromecku, nejsou tam cervene hrany mezi koreny stromecku), hrana $xy$ skvela $\equiv s(y) \geq 2s(x)$ (kdyby $\forall$ hrany byly skvele, tak $Union$ by cokoliv produkuje same skvele); mejme $x\rightarrowtail y\rightarrowtail z$ a hrana $yz$ je taktak, if pro nejake $x$ splnuje $s(z)\geq 2s(x)$; pro same skvele hrany vychazi v lemma $a=2$, tak pokud jsou nekter i taktak vyjde $a=\sqrt{2}$, ale v obou pripadech splnuje lemma. $\forall$ hrany jsou skvele/taktak, v $Update$ delam $Uni$ by size $\rightarrow$ produkuje skvele hrany. Mejme tedy 2 po sobe jdouci hrany $t\rightarrowtail r \rightarrowtail q$, kde ani jedna nezvnikla $Updatem$ (kdyby aspon 1 vznikla, je skvela a pak je ta dvojice (druha) aspon taktak), cili jsou ty modre hrany (kdyz prepojuji koreny podstromy pri zbytku $Linku$), vzdy je pripojuji pod koren. Urcite jsme museli drive pripojit $t$ pod (tehdy) koren $r$ a plati $size(r) \geq 2s(t)$, A-$size(s)\leq size(r)$: $size(r)$ bude 2nasobek puvodniho $size(s)$, protoze kvuli A a pote jsem tam pridal jeste vsechny podstromy $s$, ale kazde $s(s_i)$ je urcite $\leq size(s)$, B-$size(s)>size(r)$ nove $size(r)$ je aspon 2x vetsi puvodnimu a kazdy $s(r_i)\leq$ puvodnimu $size(r)$. Pak nejaky arg, proc je tedy pri druhe pridane modre $size(q)\geq2s(t)$ a tim padem jsou $\forall$ hrany aspon taktak $\Rightarrow$ cele je to vyvazene a cele to bezi v $O(\alpha(m,n))$.
SOFT HALDY: $key(x)$= puvod klic, $current(x)$= aktual klic, $\forall x: current(x)\geq key(x)$ ($>$ pak $x$, poniceno), halda si pamatuje oba klice (umime rict if ponicen), pro $\epsilon \in (0, 1/2]$ invariant: Halda obsahuje max $\epsilon n$ ponicenych prvku ($n$ neni \# prvku, ktere jsou aktualne v halde, ale kolik bylo do ted insertovano). Slozitost: $Insert \in O(L(1/\epsilon))$, ostatni $O(1)$ (vse amort.). If $\epsilon = 1/2n$ $\rightarrow$ nikdy se nenici prvky, ale slozitost je $O(Ln)$, if $\epsilon =1/2$ pak znici pulku prvku a slozitost je konstantni. Operace: $Init(\epsilon)$, $Insert(x,k)$ ($k$ je tedy puvod klic), $ExtractMin$ vrati prvek s min aktual klic (prvek + oba klice), $Meld$ (slije dve haldy), $Dissassemble$ rozebere na prvky pamatujici si $key$ i $current$. Aplikace: pivo pro quicksort, median prilis tezky, proto pivot, ktery lezi v prostredni tretine setridene pslpnsti: 1. $\epsilon \leftarrow 1/3$, $Init$, 2. $Insert x_1\_x_n$, 3. $n/3 \times ExtMin$, 4. $p \leftarrow current(\text{posled extrahovany prvek})$. $\exists$ aspon $n/3$ prvku, ktere $\leq p$ Dk1: $p\geq current(\forall$ vytaz prvku) tedy i $\geq key$($\forall$ vytaz prvku), tedy aspon $n/3$ prvku je mensich $\square$, ktere $>p$ Dk2: $p \leq current$ zbylych prvku v halde, zbyva nam $(2/3)n$ prvku, ale $\epsilon=1/3$, takze max $n/3$ z nich je ponicenyhc $\rightarrow p\leq key$ pro aspon $n/3$ neponicenych. Struktura: sklada se z bin stromu, $\forall v$: $l(v)$,$r(v)$= deti $v$, $rank(v)$= nikdy se nemeni, ranky synu o 1 mensi (listy na zacatku 0, pak uz nemusi), $list(v)$=seznam prvku ulozenych v tomto vrcholu, $ckey(v)$= spolecny klic prvku v $list(v)$ ($ckey$ vzdy $\geq$ vsem skutecnym klicum, aby nam niceni prvku fungovalo spravnym smerem), ($ckey(rodic)\leq ckey(dite)$), $size(v)$ = std vel seznamu (zavisi na $rank(v)$, znacime $s_{rank(v)}$). Zvolime $r=\lceil L(1/\epsilon)\rceil +5$ a $r>rank(v)$ pak std vel $list(v)$ je 1,  if $rank(v)<r$ rostou exponencialne. $s_k$ je 1 if $k\leq r$, je $\rceil (3/2) s_{k-1}\lceil$ if $k>r$ ($s_k$ expon rostou s mocninou $(3/2)$). lemmatko: $(3/2)^{k-r} \leq s_k \leq 2(3/2)^{k-r} -1$ pro $k\geq r$, Dk: Dolni odhad plati, protoze horni cela cast to pouze zvysuje, horni odhad indukci:  pro $k=r$ urcite plati, $s_k \leq (3/2)s_{k-1} +1$
$\leq (3/2)\cdot(2(3/2)^{k-r-1} -1) +1 = 2(3/2)^{k-r}-0.5$$\square$. To byly stromu. Halda vypada: seznam stromu rostoucich ruznych ranku (obousmerny seznam, rank stromu=rank korene, ckey stromu= ckey korene) (stromy vetsiho ranku nemusi byt hlubsi nez mensiho, uz muzou byt ohlodane), prvek s min aktual klicem se nachazi v koreni nektereho ze stromu, toho co ma min ckey (nemame cas projit $\forall$) $\rightarrow$ udrzujeme $SuffixMin(T)$= argmin $ckey(T')$ pro $T': rank(T')\geq rank(T)$ (kde), $SufM$ 1. stormu je global min, $USM(T)$= update suffixoveho minima: $T$ strom kde jsem mozna zmenil $ckey$ $\rightarrow \in O(\#\text{strmu doleva od T}) \leq rank(T)+1$. $Sift(v)$ volame na $x$ co neni list: 1. Dokud $v$ neni list \& $|list(v)|<size(v)$: 1A If $l(v)$ neni nebo $ckey(l(v))>ckey(p(v))$ tak $l\leftrightarrow p$, 1B: presuneme prvky z $list(l(v))$ do $list(v)$, 1C $ckey(v)\leftarrow ckey(l(v))$, 1D: If $l(v)$ list, smazeme ho, jinak $Sift(l(v))$. $Combine(x,y)$ ($x$ i $y$ maji $rank=k$) = vytvorime novy koren $z$ s $rank(z)=k+1$ a zavolame $Sift(z)$ (bacha, possible novy $ckey$ korene, musime prepocitat suffixova minima). $Meld(H_1, H_2)$: buno $H_1$ je ta s mensim rankem (rank haldy = max rank $\forall$ stromu v halde), zacnu slevat, if ranky ruzne jen zatridim ranky do $H_2$, if $T_1\in H_1$ a $T_2\in H_2$ maji $rank(T_1)=rank(T_2)$$\rightarrow Combine(T_1,T_2)$ (vlastne souc binarnich cisel) trva $O(rank(H_2))$, protoze v te chvili dojdu na konec $H_2$ a pak jeste muzu mit prenos a az nekdy se zastavim v $H_2$, od tama udelam usm (vlevo zmena $ckeys$, vpravo puvodni $H_2$). $Insert(x)=Meld(H, {v_0})$ $rank(v_0)$=0 $list(v_0)=\{x\}$. $EMin$: $T \leftarrow SuffMin(\text{1. stromu})$ $v\leftarrow root(T)$ $x \leftarrow pop(list(v))$, if $v$ je list a $list(v)$=0: tak $delete(T)$ a $USM(\text{predchudce} T)$, if $v$ not list a $|list(v)| < size(v)$ tak $Sift(v)$ a $USM(T)$, return $x$.| InvL: If ($rank(v)\leq r$: $|list(v)|=1$) \& (if $rank(v)=k>r: (1/2)s_k\leq |list(v) \leq 3s_k$ \&  $v$ neni list). Dk: 1(): 0 to neni lebo vrcholy s praznym $list$ mazu v $Sift$, dale indukci: jakmile klesne $|list(v)|=0$, volam v $EMin$ $Sift$ $(0<(1/2)\cdot 1)$ tak nacerpam prvky jednoho ze svych deti, ty maji mensi $rank$, takze indukci je \# prvku v jejich seznamu =1, jakmile si ho vezmu, uz je podm v $Sift$ uspokojena a nesosam dal. 2(): 1$\leq$: pri $Insert$ vytvorim triv haldu, u ktere to plati, nebo delam $Combine$, ale ten si hned zavola $Siftt$, nebo ubiram $EMin$ a bud zavolam $Sift$ nebo je splnena podm $|list(v)| \geq (1/2)size(v)$, ale pri ni uz 1. nerovnost plati a $Sift$ skonci, kdyz je $list(v)\geq size(v)$ (v te chvili uz 1. nerv plati) nebo je to list a jeho se nerovnost netyka. $2(): 2\leq:$ indukci podle ranku: seznam prodluzujeme pri $Insert$ ale rank=0 takze se ho nerov netyka nebo v $Sift$: ve $v$ ranku $k$ delam $Sift(v)$: $|list(v)| < s_k=size(v)$, zacnu cerpat z deti a nejvyse prenesu $3s_{k-1}$ vrcholu diky ip, ale $3s_{k-1}\leq (2/3)s_k$ $\rightarrow|list(v)|\leq 3s_k$.$\square$ InvR: \#vrcholu $ranku$ $k$ $\leq n/2^k$. Dk: $rank$ se nikdy nemeni, vrcholy $ranku$ 0 vznikaji $Insertem$, vrcholu $ranku$ 0 vznikne prave $n \rightarrow$ plati, v $Combine$ vyrabi vzdy $k+1$ $ranku$ ze dvou korenu s rank $k$ a kdykoliv prestane byt vrchol korenem, uz se nim nikdy nestane, takze zrusim-li 2 vrchy ranku $k$, tak se uz nikdy nemuzou zadneho $Comb$ zucastnit, takze $\#$ vrcholu ranku $k+1$ je $\leq$ (1/2) vrchu ranku $k \rightarrow$ indukci to vyjde.$\square$ InvC: \# ponic prvku $\leq \epsilon n$. Dk: pp muzou byt jen pro vrcholy $rank > r$, tak uvazujme nejhorsi pripad: $\forall$ prvky v seznamech takovych vrcholu jsou ponicene: \#pp $\leq \Sigma_{k>r}(n/2^k)\cdot3\cdot2(3/2)^{k-r}$ (InvR, InvL, pozorovani pro $s_k$) $\leq (6n/2^r) \cdot \Sigma_{k>r}((1/2)^{k-r}\cdot (3/2)^{k-r}) = (..) \cdot \Sigma{i\geq1} (3/4)^i$ $= (..)\cdot (\Sigma{i\geq0}(..) - 1) \leq (..)\cdot (4-1)$
$= 18n/2^r$ ($2^r \geq 32\cdot(1/\epsilon)$) $\leq 18n/(32\cdot 1/\epsilon) < \epsilon n$ $\square$ Korektnost done! Slozitost: (amort $Insert \in O(r)$ ostatni amort $O(1)$). $\Phi$ je souctem []: halda ranku $k\rightarrow$ [$k+1$]; strom s korenem $x\rightarrow$ [$(r+2)\cdot del(x)]$ kde $del(x)$= kolik prvku jsme ze seznamu v koreni smazali od posledniho $Sift$ a nedoslo-li k $Siftu$ tak od vytvoreni seznamu; koren ranku $k\rightarrow$ [$k+7$]; vrchol co neni koren $\rightarrow$ [1]. $Sift(v)$ (kde $rank(v)=k$): staci dokazat slozitost 1 iterace smycky (bez rekurz volani, to stoji 1 + cena smycky): do $list(v)$ presouvam $list(s)$: A: $|list(s)|<size(s)/2$ pak $s$ je list tedy v kroku 1D ho smazu $\rightarrow$ $\Phi_\Delta = -1$ z toho zaplatim co jsem ve smycce udelal, B: $|list(s)| \geq size(s)/2$ tak rozpocitame mezi prvky $list(s)$, protoze $|list(s)| \geq s_{k-1}/2$ tak kazdemu prvku nauctuji $\leq 2/s_{k-1}$, prvky jenom stoupaji, Celkove prvek plati= za probublani prvnich $r$ (vcetne) hladin plati [$r+1$] a vsechny vyssi ranky [$2\Sigma_{k>r}(1/s_k) \leq 2\Sigma_{i\geq0}(2/3)^i \leq \text{konstanta}$] $\Rightarrow$ 1 prvek plati $\in O(r+1+\text{konst})\in O(r)$ a tedy amort cena $Siftu \in O(1)$. $Combine$: $k \cup k \rightarrow k+1$ stoji konstantu do ktere se vejde i zavolani $Siftu$, $\Phi$ klesne o mozna $-(r+2)\cdot del(x)$ ale urcite o $-2\cdot(k+7)$, stoupne o +$2$ vnitrni vrcholy, +$(k+1)+7$, $del$ noveho korene je 0, mohl jsem zvysit $rank$, takze +1 a prace stoji +1, takze $k+12-(2(k+7))=-k-2$, zaplatim a jeste zbyde k+2 a z toho zpalatim $usm$ (trva rank+1, coz odpovida (k+1)+1). $Meld \in O(rank(H_1))=k$, coz zaplatim z $\Phi$, protoze zanikne halda $ranku$ $k$, pak parkrat $Combine$ (zaplat sami), nakonec $usm$ -bud jsem skoncil $Combine$ a ten to zaplati nebo -mi dosel seznam $H_1$, pak $usm\in O(k)$ tedy zaplatim z poklesu $\Phi$.
$ExtrMin$: $v$ koren obsahujici min, if stale $|list(v)|\geq (1/2)size(v)$ tak jsme vykonali praci $O(1)$ a $\Delta\Phi$ = $(r+2)$, protoze $del$ =+ 1 a to nauctuju prvku, ktery jsem prave smazal $\rightarrow$ zase $O(r)$ na prvek; if $|list(v)|<(1/2)size(v)$: A- $v$ je list a $|list(v)|>0$, tak uz nedelame nic a pouze se nam zvednul $del$ za smazany syny, ale to jim nauctujeme a nebo $|list(v)|=0$, pak mazeme cely strom a delame $usm$ a $\Phi$ klesne o $rank(v)+7$ z cehoz zaplatime praci co jsme udelali a i $usm$; B- $v$ neni list tak nekdy v minulosti po poslednim $Sift$ byl $list(v)$ dlouhy $\geq size(v)$ (protoze $Sift$ skoncil), ted klesnul pod $(1/2)size(v)$, takze jsme ze seznamu smazali aspon $\geq (1/2)size(v)$ prvku tedy $del\geq(1/2)size(v)$ a prave delam v $EMin$ $Sift$, takze skoci $del$ na 0 takze $\Delta\Phi \leq -((r+2)/2 \cdot size(v))$ a da se ukazat, ze vyraz v zavorce je aspon $k+1$, takze uvolni aspon $k+1$ coz je to, co opet zaplati $usm$.

|||||
{4.2. Robust contractions} {4.2.1. Definition.} A subgraph $C \subseteq G$ is \textit{contractible} iff for every pair of edges $e, f \in \delta(C)$ there exists a path in $C$ connecting the endpoints of the edges $e, f$ such that all edges on this path are lighter than either $e$ or $f$. Here $\delta(C)$ denotes the set of edges having exactly one endpoint in $C$. {4.2.3. Lemma.} \textit{(Generalized contraction)} When $C \subseteq G$ is a contractible subgraph, then $\mathrm{msf}(G) = \mathrm{msf}(C) \cup \mathrm{msf}(G/C)$. {4.2.4. Notation.} When $G$ is a weighted graph and $R$ a subset of its edges, we will use $G \Uparrow R$ to denote an arbitrary graph obtained from $G$ by increasing the weights of some of the edges in $R$. Whenever $C$ is a subgraph of $G$, we will use $R^C$ to refer to the edges of $R$ with exactly one endpoint in $C$ (i.e., $R^C = R \cap \delta(C)$). {4.2.5. Lemma.} \textit{(Robust contraction, Chazelle)} Let $G$ be a weighted graph and $C$ its subgraph contractible in $G \Uparrow R$ for some set $R$ of edges. Then $\mathrm{msf}(G) \subseteq \mathrm{msf}(C) \cup \mathrm{msf}((G/C) \setminus R^C) \cup R^C$. {4.2.7. Algorithm.} \textit{(Partition a graph to a collection of contractible clusters)} Input: A graph $G$ with an edge comparison oracle, a parameter $t$ controlling the size of the clusters, and an accuracy parameter $\varepsilon$. 1. Mark all vertices as ``live''. 2. $\mathcal{C} \leftarrow \emptyset$, $R^C \leftarrow \emptyset$. \textit{(Start with an empty collection and no corrupted edges.)} 3. While there is a live vertex $v_0$: 4. $T = \{v_0\}$. \textit{(the tree that we currently grow)} 5. $K = \emptyset$. \textit{(edges known to be corrupted in the current iteration)} 6. Create a soft heap with accuracy $\varepsilon$ and \textit{Insert} the edges adjacent to $v_0$ into it. 7. While the heap is not empty and $|T| \le t$: 8. \textit{DeleteMin} an edge $uv$ from the heap, assume $u \in T$. 9. If $uv$ was corrupted, add it to $K$. 10. If $v \in T$, drop the edge and repeat the previous two steps. 11. $T \leftarrow T \cup \{v\}$. 12. If $v$ is dead, break out of the current loop. 13. \textit{Insert} all edges incident with $v$ to the heap. 14. $\mathcal{C} \leftarrow \mathcal{C} \cup \{G[T]\}$. \textit{(Record the cluster induced by the tree.)} 15. \textit{Explode} the heap and add all remaining corrupted edges to $K$. 16. $R^C \leftarrow R^C \cup K^T$. \textit{(Record the ``interesting'' corrupted edges.)} 17. $G \leftarrow G \setminus K^T$. \textit{(Remove the corrupted edges from $G$.)} 18. Mark all vertices of $T$ as ``dead''. Output: The collection $\mathcal{C}$ of contractible clusters and the set $R^C$ of corrupted edges in the neighborhood of these clusters. {4.3. Decision trees} {4.3.3. Lemma.} $D(m,n) \le 4/3 \cdot n^2$. {4.3.5. Lemma.} \textit{(Construction of optimal decision trees)} An optimal MST decision tree for a graph $G$ on $n$ vertices can be constructed on the Pointer Machine in time $\mathcal{O}(2^{2^{4n^2}})$. {4.3.7. Lemma.} The decision tree complexity $D(m,n)$ of the MSF satisfies: 1. $D(m,n) \ge m/2$ for $m,n > 2$. 2. $D(m',n') \ge D(m,n)$ whenever $m' \ge m$ and $n' \ge n$. {4.3.8. Definition.} Subgraphs $C_1, \dots, C_k$ of a graph $G$ are called the \textit{compartments} of $G$ iff they are edge-disjoint, their union is the whole graph $G$ and $\mathrm{msf}(G) = \bigcup_i \mathrm{msf}(C_i)$ for every permutation of edge weights. {4.3.9. Lemma.} The clusters $C_1, \dots, C_k$ generated by the Partition procedure of the previous section (Algorithm 4.2.7) are compartments of the graph $H = \bigcup_i C_i$. {4.3.10. Lemma.} Let $C_1, \dots, C_k$ be compartments of a graph $G$. Then there exists an optimal MSF decision tree for $G$ that does not compare edges of distinct compartments. {4.3.11. Lemma.} Let $C_1, \dots, C_k$ be compartments of a graph $G$. Then $D(G) = \sum_i D(C_i)$. {4.3.12. Corollary.} If $C_1, \dots, C_k$ are the clusters generated by the Partition procedure (Algorithm 4.2.7), then $D(\bigcup_i C_i) = \sum_i D(C_i)$. {4.3.13. Corollary.} $2D(m,n) \le D(2m,2n)$ for every $m,n$.
||||
Zkrátka vezmeš skupinu posloupností délky 3, víš, že všechny mají přesně 3 prvky, a prostě na ně pustíš tříkolový Radix Sort.
Ale jak spočítat délku a roztřídit je podle ní, když na Pointer Machine nemáme čísla a nemůžeme použít proměnnou `length`?
Opět nás zachrání naše Měřítko (Yardstick) a jeho prázdné přihrádky! Dělá se to takto:
Algoritmus: Třídění podle délky na PM
Představ si, že Měřítko je dlouhý spojový seznam prázdných uzlů. První uzel reprezentuje délku 1, druhý délku 2, atd. (My ta čísla nevidíme, ale fyzicky to tak leží v paměti).
1. Měření "krok za krokem" (Zipping)
Vezmeme první posloupnost, kterou chceme změřit.
* Položíme jeden ukazatel (prst) na začátek naší posloupnosti.
* Položíme druhý ukazatel (prst) na první uzel Měřítka.
* Nyní posuneme OBA prsty současně o jeden krok dopředu.
* Toto opakujeme v `while` cyklu, dokud prst na naší posloupnosti nenarazí na konec (null).
2. Odložení do přihrádky
Jakmile naše posloupnost skončila, podíváme se, kde přesně skončil náš druhý prst v Měřítku. Ten uzel v Měřítku teď fyzicky reprezentuje délku naší posloupnosti!
* Vezmeme celou naši posloupnost a pověsíme ji do `bucketu` (přihrádky) u tohoto uzlu v Měřítku.
* Toto uděláme úplně pro všechny posloupnosti, které chceme zpracovat. Stojí to čas přesně úměrný součtu délek všech posloupností (což je stále naše lineární $\mathcal{O}(\sum |S_i|)$).
3. Sklizeň
Jakmile jsou všechny posloupnosti změřeny a odloženy, jednoduše projdeme Měřítko od prvního uzlu dál.
* Přijdeme k prvnímu uzlu. Vysypeme jeho přihrádku. Získáme skupinu všech posloupností, které mají délku 1.
* Přijdeme ke druhému uzlu. Vysypeme přihrádku. Získáme skupinu všech posloupností o délce 2.
* atd
Výsledek
Získáš krásně oddělené hromádky. Hromádku "dvojek", hromádku "trojek"... A na každou tuto hromádku teď můžeš nezávisle a v klidu spustit tu unifikaci (rozhazování podle 1. znaku, 2. znaku...), kterou jsme probírali minule, s jistotou, že ti žádná posloupnost neskončí dřív než ostatní ve stejné skupině.
Tady jsem pro tebe připravil interaktivní vizualizaci, kde si přesně tento proces "měření délky na Pointer Machine" můžeš odkrokovat:
Tímto "hloupým" posouváním dvou ukazatelů najednou jsme plnohodnotně nahradili funkci `length()` a vytvořili regulérní Bucket Sort podle délek! Dává to smysl?
Tohle je přesně ta chvíle, kdy se zachraňuje ta naše bájná lineární časová složitost! Tvůj profesor uhodil hřebíček na hlavičku.
Představ si situaci: Máš Měřítko (Yardstick), které je dlouhé třeba 1000 uzlů (protože ve stromu je 1000 vrcholů). A teď jdeš zpracovávat hromádku posloupností, které mají délku 2. Těchto posloupností v té hromádce je ale jenom 5.
V čem je problém? (Proč to nejde naivně)
Vezmeš těch 5 posloupností a podle jejich prvního znaku je rozházíš do přihrádek v Měřítku. Strávíš tím 5 kroků.
Pak ale potřebuješ ty přihrádky zase vysypat, abys posloupnosti seřadil. Kdybys naivně procházel celé Měřítko od začátku do konce a hledal, kam jsi těch 5 posloupností dal, musel bys zkontrolovat všech 1000 uzlů.
Zničil bys tím lineární čas! Práce by netrvala $\mathcal{O}(5)$, ale $\mathcal{O}(1000)$.
Řešení: Udržování aktivních přihrádek
Algoritmus to řeší tak, že si pamatuje, do kterých šuplíků něco dal, aby pak nemusel otevírat ty prázdné. Dělá to pomocí malého pomocného spojového seznamu, kterému se říká Aktivní přihrádky (Active Buckets).
Funguje to takto krok za krokem:
1. Rozhazování (Scattering) s evidencí:
* Máš prázdný pomocný seznam `AktivniPrihradky`.
* Vezmeš první posloupnost a podíváš se na její znak (ukazatel na uzel $X$ v Měřítku).
* Podíváš se do uzlu $X$. Je jeho přihrádka aktuálně prázdná?
* ANO: To znamená, že do uzlu $X$ dáváme něco poprvé v tomto kole. Přidáme tedy odkaz na uzel $X$ do našeho seznamu `AktivniPrihradky`. Poté do přihrádky u $X$ vložíme naši posloupnost.
* NE: V přihrádce už něco je. Uzel $X$ už v seznamu aktivních přihrádek zapsaný máme. Jen tam tedy přihodíme naši posloupnost a jdeme dál.
2. Sklizeň (Gathering) pouze z aktivních:
Jakmile jsi rozházel všech 5 posloupností, vůbec se nedíváš na celé Měřítko.
Místo toho vezmeš svůj malý pomocný seznam `AktivniPrihradky`. V něm jsou zapsané (například) jen 3 uzly z Měřítka, které jsi reálně použil.
Projdeš pouze tyto 3 uzly:
Z uzlu vysypeš jeho přihrádku (čímž se ti posloupnosti seřadí).
DŮLEŽITÉ: Přihrádku u uzlu zase hned nastavíš na `null` (vyprázdníš ji), aby byla čistá pro další kola.
Nakonec seznam `AktivniPrihradky` smažeš/vyprázdníš.
Díky tomuto triku trvá fáze sklizně naprosto stejně dlouho jako fáze rozhazování. Rozházel jsi 5 prvků? Sklidíš maximálně 5 aktivních přihrádek. Čas strávený v jednom kole (pro jednu pozici v řetězci) je tedy striktně $\mathcal{O}(\text{počet zpracovávaných posloupností})$, naprosto nezávisle na tom, jak obrovské je Měřítko.
Když pak sečteš tento čas přes všechny pozice všech posloupností, dostaneš přesně tu bájnou složitost $\mathcal{O}(\sum |S_i|)$.

\end{multicols*}
\end{document}